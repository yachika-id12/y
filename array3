#include<stdio.h>
#include<conio.h>
void main()
{
void differ(int arr[],int n)
{
int i=0,diff=0
int max=arr[0]
int secmax=a[0]
for(i=0;i<n;i++)
{
if(arr[i]> max)
{
secmax=max;
max=arr[i];
}
void main()
{
int n,i,arr[0];
printf("enter the size of array");
scanf("%d",&n);
printf("enter the elements of array");
for(i=0;i<n;i++)
{
scanf("%d",&arr[i]);
}
differ(arr,n);
getch();
}
