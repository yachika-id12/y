#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    int i,j,n;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++){
    scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++){
        if(a[0]==a[1]){
            printf("%d",a[i]);
            break;
    }
    int count=0;
    for(j=i+1;j<n;j++){
        if (a[i]==a[j+1])
        printf("%d ",a[i]);
    }
    }
    
        return 0;
}
